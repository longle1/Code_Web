HTML DOM có 3 thành phần chính:
    + Elements: ID, class, tag, CSS selector, HTML collection
    + Attribute
    + Text


HTML DOM không phải là của js mà HTML DOM là quy chuẩn của w3c còn js chỉ tạo ra những bộ công cụ cho HTML DOM đi vào được trong js